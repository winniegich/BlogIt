BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Blogs] ADD [isPermanentlyDeleted] BIT NOT NULL CONSTRAINT [Blogs_isPermanentlyDeleted_df] DEFAULT 0;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
